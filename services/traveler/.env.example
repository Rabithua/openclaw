# Traveler 环境变量配置
# 复制为 .env 并填入实际值

# ============================================
# 必需配置
# ============================================

# OpenClaw Gateway（必需）
# Traveler 现在完全依赖 OpenClaw 进行内容筛选和发布
OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=REPLACE_ME

# Rote API Access
ROTE_API_BASE=https://api.rote.ink/v2/api
ROTE_OPENKEY=


# ============================================
# 可选配置 - Submit API
# ============================================

# 如果需要启动 HTTP 服务器接收外部提交
TRAVELER_PORT=8788

# API Token 认证（推荐用于脚本调用）
TRAVELER_API_TOKEN=your-secret-api-token-here

# HMAC Secret 认证（推荐用于 webhook，可选）
TRAVELER_HMAC_SECRET=your-hmac-secret-key-here


# ============================================
# 可选配置 - 内置定时任务（server 模式）
# ============================================

# 定时执行间隔（分钟），设置 > 0 即开启
TRAVELER_SCHEDULE_INTERVAL_MINUTES=0

# server 启动时是否立刻执行一次（true/false）
TRAVELER_SCHEDULE_RUN_ON_START=true

# SQLite 去重数据库路径（默认 .local/state/traveler.db）
# Docker 下建议保持默认，目录会挂载到持久卷
TRAVELER_DB_PATH=.local/state/traveler.db
