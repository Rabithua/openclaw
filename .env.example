# OpenClaw Services 环境变量配置
# 复制为 .env 并填入实际值

# ============================================
# OpenClaw Gateway（两个服务都需要）
# ============================================
OPENCLAW_GATEWAY_URL=https://openclaw-gateway.openclaw-upstream.orb.local
OPENCLAW_GATEWAY_TOKEN=xxxxxx

# ============================================
# Traveler 配置
# ============================================
ROTE_API_BASE=https://api.rote.ink/v2/api
ROTE_OPENKEY=xxxx

TRAVELER_PORT=8788

# 可选：API 认证（仅当需要使用 /traveler/submit 接口时）
# TRAVELER_API_TOKEN: 用于 X-API-Token 头部认证
# 用法: curl -H "X-API-Token: $TRAVELER_API_TOKEN" http://localhost:8788/traveler/submit
TRAVELER_API_TOKEN=

# 可选：HMAC 签名认证（仅当需要使用 /traveler/submit 接口时）
# TRAVELER_HMAC_SECRET: 用于 X-Signature 头部 HMAC-SHA256 签名验证
# 用法: 计算 body 的 HMAC-SHA256，加入 X-Signature: sha256=<hex> 头部
TRAVELER_HMAC_SECRET=

# ============================================
# Webhookd 配置
# ============================================
WEBHOOKD_PORT=8787
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret
IGNORE_GITHUB_ACTORS=
GITHUB_REPLY_SIGNATURE=— replied by OpenClaw assistant
DEDUPE_TTL_MS=3600000
